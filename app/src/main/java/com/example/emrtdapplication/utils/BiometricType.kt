package com.example.emrtdapplication.utils

/**
 * Enumeration to determine the type of biometric feature contained in a [BiometricDataBlock]
 */
enum class BiometricType {
    FACE, IRIS, FINGERPRINT
}