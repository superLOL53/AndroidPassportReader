package com.example.emrtdapplication

/**
 * Shows the certification revocation status for certificates signed by a CSCA
 */
enum class CertificationRevocationStatus {
    UNDETERMINED, REVOKED, UNREVOKED
}